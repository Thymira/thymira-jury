# adapters/

Adapters translate an external client (CLI, VS Code/Cursor, OpenCode, Claude Code, Codex, generic) into Thymira Agent API calls. They contain no THY/MIRA logic and never own run state. The CLI is the first official interface (MVP); the others are phase 2.

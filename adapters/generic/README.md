# adapters/generic

Generic adapter: translates any external agent / IDE into Thymira Agent API calls (run, plan, resume, delegate, inspect, experiment, audit, approve, reject).

| | |
|---|---|
| Owner (MVP roadmap) | unassigned |
| Priority | P2 (phase 2) |

Placeholder: nothing is implemented here yet. See `docs/architecture/e2e-baseline-v2.md` for the target design and `docs/roadmap/` (`product-final.md` and `mvp-minimum.md`) for when this area starts.

# services/sandbox-manager

Sandbox Manager: ephemeral, non-root, resource-limited execution environments for Python/R/SQL. The MVP uses a controlled local subprocess behind the Tool Manager instead.

| | |
|---|---|
| Owner (MVP roadmap) | P3 (Tools / Execution / MLflow) |
| Priority | P1 (basic) / P2 (distributed) |

Placeholder: nothing is implemented here yet. See `docs/architecture/e2e-baseline-v2.md` for the target design and `docs/roadmap/` (`product-final.md` and `mvp-minimum.md`) for when this area starts.

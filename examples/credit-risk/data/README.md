# Demo data

The MVP demo uses the German credit data set already tracked in this repository as
`data/german_credit.csv`. Copy or symlink it here as `applications.csv` when running the example;
do not commit a second copy.
